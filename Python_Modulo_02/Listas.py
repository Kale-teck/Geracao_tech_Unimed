#uma lista tem a capacidade de armazenar qualquer item
#uma lista é mutável

frutas = ['Laranja', 'Maçã', 'Uva'] #exemplo de declaração de lista

carros = [] #uma lista pode ser declarada vazia

letras = list('abcd') #declaração usando a palavra-chave list

numeros = list(range(10)) #cria uma lista com cada elemento retornado pela função range()

automoveis = ['strada', 'nova', 11200000, 2022, 'RN', True] #lista com vários tipos

#lista é uma sequencia de objetos, por isso podemos usar índices para localizar cada objeto da sequência

print(automoveis[-1]) #retorna o último valor da lista
print(automoveis[-2]) #retorna o penúltimo valor da lista

